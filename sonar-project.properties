# SonarQube Cloud project configuration for HoneyDrunk.Observe.
# Project imported via the SonarQube Cloud GitHub App; quality gate runs in
# job-sonarcloud.yml (HoneyDrunk.Actions) on every PR and push to main.
#
# This file lives at the repo root next to HoneyDrunk.Observe.slnx — Observe's
# pr.yml runs the reusable workflow with working-directory: '.' so this is
# where the scanner discovers project configuration.
#
# Observe is a Seed-phase Node today — only Abstractions exist in src/.
# As runtime + provider packages land, they are picked up automatically
# via the src/ sonar.sources glob below.

sonar.organization=honeydrunkstudios
sonar.projectKey=honeydrunkstudios_HoneyDrunk.Observe
sonar.projectName=HoneyDrunk.Observe

# Sources / tests — repo uses the src/ + tests/ split
sonar.sources=src
sonar.tests=tests

# Coverage (consumed by job-sonarcloud.yml after Cobertura → OpenCover conversion)
sonar.cs.opencover.reportsPaths=**/coverage.opencover.xml

# Exclusions — generated code and build outputs
sonar.exclusions=**/obj/**,**/bin/**,**/*.Designer.cs

# Coverage exclusions — Abstractions package contains interfaces only
sonar.coverage.exclusions=**/src/HoneyDrunk.Observe.Abstractions/**

# New Code definition — inherits from organization default (30 days)
